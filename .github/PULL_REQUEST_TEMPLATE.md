## What changed

## Why

## Checklist

- [ ] `cargo test --workspace` and `cargo clippy --workspace --all-targets -- -D warnings` pass
- [ ] No comments added to Rust sources; reasoning is in names, types, tests or `weights.toml`
- [ ] No new dependency, or `crates/malmathic/tests/audited-dependencies.txt` updated after reading the crate
- [ ] A behaviour change comes with a synthetic-volume test
